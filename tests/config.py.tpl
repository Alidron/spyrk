login_password = ('username', 'password')
# You need both!
login_token = ('access_token', )

target_device = 'device_name'

# For available functions and variables we assume a basic Tinker app run on it.
# The test will write 'HIGH' and 'LOW' to 'D7'.
